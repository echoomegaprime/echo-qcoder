
# SECURITY_REPORT

No raw shell/paths/PIDs/tokens in responses. workspace_key allowlist only. shell:false spawn. OAuth scopes. Idempotency + expected_revision. Rate limit. Redacted transcripts. LOCAL_DEV_AUTH blocked in production.

Fixes: Windows Temp realpath test; verify-plugin JSON -AsHashtable.
Open: production OAuth binding BLOCKED BY EXTERNAL DEPENDENCY.

