[Unit]
# The governed 131072-token Qwen runner owns both FORGE GPUs while this
# persistent lease marker exists.  A failed Condition is a clean skipped start,
# so external enabled-unit reconcilers cannot race Qwen or create a restart
# storm by repeatedly invoking TitleHound.
ConditionPathExists=!/etc/echo/qwen-dual-gpu.lease
